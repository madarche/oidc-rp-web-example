<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{{app_title}}</title>
    <link rel="icon" href="{{app_icon_path}}" type="image/png"/>
  </head>
  <body>
    <h1>{{app_title}}</h1>

    <div>
      <hr/>
      Config info to never display in production:
      <ul>
        {% for key, val in config_info_not_for_prod %}
        <li><code>{{key}}</code>: <code>{{val}}</code></li>
        {% endfor %}
      </ul>
      <hr/>
    </div>

    {% block content %}{% endblock %}

  </body>
</html>
